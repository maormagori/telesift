#!/bin/sh
set -eu

if ! command -v gitleaks >/dev/null 2>&1; then
  echo "gitleaks is required before committing: https://github.com/gitleaks/gitleaks#installing" >&2
  exit 1
fi

exec gitleaks git --pre-commit --redact --staged --verbose
